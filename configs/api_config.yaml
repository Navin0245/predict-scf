# API serving layer configuration

server:
  host: 0.0.0.0
  port: 8000
  workers: 1
  log_level: info

model:
  registry_uri: mlruns
  model_name: scf-surrogate
  model_version: 1

validation:
  strict_dnv_bounds: true
